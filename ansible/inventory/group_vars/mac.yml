# group_vars for the `mac` inventory group. Seeded from the live
# machine by `coily ansible-mac-seed` (scripts/ansible/seed_mac_brew.py).
# Additive: ansible ensures these are present, never uninstalls.
# Curate by hand after seeding - this is the fleet's declared baseline.

# agent-compose source selection for this machine class. The `mac` group is
# personal machines, so they take the role default: the public base plus the
# private overlay, composed into the global context every harness loads. A work
# mac joining the fleet belongs in its own group and overrides
# `agent_compose_sources` to the public base alone, never the private overlay.

# Third-party taps (core/cask omitted). Each entry is {name, url?}. Private
# Forgejo taps carry an explicit url - the bare name resolves to
# github.com/<user>/homebrew-<repo>, which does not exist for them. Public
# taps omit url. The coilysiren/* org migrated to coilyco-flight-deck/*;
# coily has not moved yet, agent-guard was renamed to ward (now its own tap).
homebrew_taps:
  - name: coilyco-flight-deck/otel-a2a-relay-cli
    url: https://forgejo.coilysiren.me/coilyco-flight-deck/otel-a2a-relay-cli
  - name: coilyco-flight-deck/repo-recall
    url: https://forgejo.coilysiren.me/coilyco-flight-deck/repo-recall
  - name: coilyco-flight-deck/session-lattice
    url: https://forgejo.coilysiren.me/coilyco-flight-deck/session-lattice
  - name: coilyco-flight-deck/ward
    url: https://forgejo.coilysiren.me/coilyco-flight-deck/ward
  - name: coilysiren/coily
    url: https://forgejo.coilysiren.me/coilysiren/coily
  - name: getsentry/tools
  # homebrew/services dropped: the tap was deprecated and emptied upstream
  # (brew services merged into core), so tapping it now errors. The `brew
  # services` command still works tap-free.
  - name: openhue/cli
  - name: steipete/tap
  - name: terraform-linters/tap

# Top-level formulae (brew leaves), not their dependencies.
homebrew_installed_packages:
  - asdf
  - ast-grep
  - atuin
  - awscli
  - bat
  - bottom
  - coilyco-flight-deck/otel-a2a-relay-cli/o2r
  # repo-recall skipped: its formula pins tag v0.45.0 to commit c5f0e8e, but
  # that tag was re-pointed upstream to 5041759, so brew's tag-integrity check
  # fails and aborts the whole batch. Re-enable once the formula's pinned sha
  # is corrected upstream in the coilyco-flight-deck/repo-recall tap.
  # - coilyco-flight-deck/repo-recall/repo-recall
  # session-lattice-puller skipped: its formula declares a dependency on the
  # dead coilysiren/session-lattice org path (pre-migration), which is
  # unresolvable, and re-adding the old tap collides with the new one (same
  # repo via redirect => duplicate formula). Re-enable once the puller's
  # dependency is fixed upstream in the coilyco-flight-deck/session-lattice tap.
  # - coilyco-flight-deck/session-lattice/session-lattice-puller
  - coilyco-flight-deck/ward/ward
  # coilysiren/agent-guard/agent-guard dropped: orphaned keg, the tap renamed
  # the formula to `ward` (coilyco-flight-deck/infrastructure#197). `brew
  # uninstall coilysiren/agent-guard/agent-guard` on this Mac to clear it.
  - coilysiren/coily/coily
  - cryptography
  - diff-pdf
  - direnv
  - dive
  - docker
  - docker-completion
  - doggo
  - duf
  - dust
  - duti
  - erdtree
  - eza
  - fd
  - flyctl
  - fping
  - gh
  - git-delta
  - git-filter-repo
  - git-lfs
  - glow
  - golangci-lint
  - googleworkspace-cli
  - gradle
  - gradle@7
  - grpcurl
  - grype
  - helm
  - httpie
  - imagemagick
  - jq
  - json2tsv
  - librsvg
  - lima
  - maven
  - mcp-grafana
  - micro
  - mise
  - mkcert
  - mtr
  - navi
  - nethogs
  - nmap
  - nuget
  - oath-toolkit
  - openhue/cli/openhue-cli
  - openjdk@11
  - ouch
  - pinentry-mac
  - pipx
  - powershell
  - procs
  - pyenv
  - python@3.12
  - repomix
  - ruby
  - ruff
  - rust
  - scc
  - scrcpy
  - sd
  - starship
  - steipete/tap/mcporter
  - stern
  - tailscale
  - tailwindcss
  - tfenv
  # tflint left homebrew-core; it ships from the terraform-linters tap now,
  # so the bare name no longer resolves. Qualified to its tap.
  - terraform-linters/tap/tflint
  - tokei
  - tree
  - trippy
  - trufflehog
  - uutils-coreutils
  - uutils-diffutils
  - uutils-findutils
  - uv
  - watch
  - watchexec
  - wget
  - yq
  - zlib
  - zoxide

# GUI apps + tools installed as casks.
homebrew_cask_apps:
  - android-platform-tools
  - aptakube
  - blender
  # codex skipped: already installed as an npm global
  # (/opt/homebrew/bin/codex -> @openai/codex), and the cask refuses to
  # clobber that binary. The npm install is the working harness; leave it.
  # - codex
  - corelocationcli
  - docker
  - docker-desktop
  - freelens
  - gcloud-cli
  - google-chrome
  - hammerspoon
  - headlamp
  - intellij-idea-ce
  - ngrok
  - obsidian
  - orbstack
  # powershell dropped from casks: there is no `powershell` cask (it ships as
  # the `powershell` formula, already declared above). The bogus cask entry
  # failed the casks task.
  - proxyman
  - tableplus
  - tailscale-app
  - visual-studio-code
  - warp
  - warp@preview
