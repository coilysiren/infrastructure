# Inventory for the coilysiren fleet: localhost over a local connection. The
# freshen play classifies it into mac/linux by OS (group_by). See docs/ansible.md.
all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"
