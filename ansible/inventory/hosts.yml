# Bare localhost; sync play group_by's OS + host class at run time. See docs/ansible.md.
all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"
