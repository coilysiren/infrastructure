# Fleet inventory: mac = macOS hosts, work = employer-owned. See docs/ansible.md.
all:
  children:
    mac:
      children:
        work:
          hosts:
            localhost:
              ansible_connection: local
              ansible_python_interpreter: "{{ ansible_playbook_python }}"
