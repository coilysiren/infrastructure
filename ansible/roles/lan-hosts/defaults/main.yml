# Inert by default: the role is committed for review but writes nothing until
lan_hosts_enabled: false

lan_hosts_pins:
  - { name: kai-server, ip: 192.168.0.194 }

lan_hosts_home_ipv4_prefix: "192.168.0."
