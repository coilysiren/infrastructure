# Repo-data collectors authored in agentic-os-kai. This role is the rollout layer
repo_data_kai_root: "{{ (repos_root | regex_replace('^~', ansible_env.HOME)) | dirname }}/coilyco-bridge/agentic-os-kai"

repo_data_targets:
  - build-catalog-graph
  - sync-repo-registry
  - compile-repo-digests
