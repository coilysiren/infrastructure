# Public front door for the Tangled knot (host systemd service on :5555) via
# Traefik + cert-manager DNS-01. Details: docs/k3s-deploy-notes.md.
---
apiVersion: v1
kind: Namespace
metadata:
  name: tangled
---
apiVersion: v1
kind: Service
metadata:
  name: tangled-knot
  namespace: tangled
spec:
  ports:
    - name: http
      port: 5555
      targetPort: 5555
      protocol: TCP
---
# Selector-less Service: Endpoints pinned by hand to kai-server's RFC1918 LAN IP
# so the off-cluster host-systemd knot is reachable as a normal Service.
apiVersion: v1
kind: Endpoints
metadata:
  name: tangled-knot
  namespace: tangled
subsets:
  - addresses:
      - ip: 192.168.0.194
    ports:
      - name: http
        port: 5555
        protocol: TCP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tangled-knot
  namespace: tangled
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - tangled.coilysiren.me
      secretName: tangled-knot-tls
  rules:
    - host: tangled.coilysiren.me
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: tangled-knot
                port:
                  number: 5555
