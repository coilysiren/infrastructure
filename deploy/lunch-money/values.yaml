# Real kai-server Helm values for lunch-money-k8s (chart source: coilyco-flight-deck/

image:
  repository: docker.io/coilysiren/lunch-money-k8s
  tag: latest
  pullPolicy: Always

lunchMoney:
  existingSecret: lunch-money-token

nodeSelector:
  kubernetes.io/hostname: kai-server

resources:
  requests:
    cpu: 50m
    memory: 128Mi
  limits:
    memory: 256Mi
